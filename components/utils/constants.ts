export enum Priority {
    PRIMARY = 'primary',
    SECONDARY = 'secondary'
}

export enum Alignment {
    CENTER = 'center',
    START = 'start',
    END = 'end'
}

export enum Order {
    REVERT= 'revert',
    NORMAL = 'normal'
}

export enum Pages {
    INITIAL = '/'
}

export enum TagsLabels {
    ALL = 'all'
}
