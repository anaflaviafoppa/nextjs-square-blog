import React from 'react';
import Layout from '../components/layout';

function SobreNos({preview}) {
    return (
        <Layout preview={preview}>
            <div>Sobre nós</div>
        </Layout>
    );
}

export default SobreNos;
